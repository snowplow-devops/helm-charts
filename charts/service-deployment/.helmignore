# Patterns to ignore when building packages.
.git/
.gitignore
.DS_Store
*.swp
*.bak
*.tmp
*.orig
*~
.project
.idea/
.vscode/

# Schema test fixtures: exercised by scripts/helm-schema-test.sh in CI,
# not needed by consumers of the packaged chart.
tests/
